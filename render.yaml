services:
  - type: web
    runtime: node
    name: modern-chat
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_VERSION
        value: "18"
      - key: PORT
        value: 10000